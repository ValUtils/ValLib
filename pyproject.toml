[project]
name = "ValLib"
version = "1.0.2"
readme = "README.md"
authors = [{ name = "PWall" }]
license = {text = "GPL-3.0"}
keywords = ["valorant", "api", "auth"]
dependencies = [
    "requests",
    "jwt",
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/ValUtils/ValLib"
